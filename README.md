# rusmasssurveilance

Проект представляет собой веб-приложение на Next.js, создающее интерактивную карту метро с панелью управления и пограничным центром.

## 📦 Описание

Это SPA, построенное на **Next.js 13** (app directory) и **TypeScript**. Оно включает:

- динамическую карту метро (`metro-map.tsx`), поддерживающую линию, узлы и анимацию частиц;
- разделы подключения и информации о линиях (`connections-section.tsx`, `line-details.tsx`);
- центральный хаб (`central-hub.tsx`) как основное представление приложения;
- настраиваемую тему с провайдером (`theme-provider.tsx`);
- обширную библиотеку UI-компонентов в `components/ui` для интерфейса.

Проект ориентирован на расширяемость: компоненты легковесны, хук `use-mobile` определяет мобильную версию, а `use-toast` работает с уведомлениями.

## 🛠 Технологии

- **Next.js 13+** (app router)
- **React 18**
- **TypeScript**
- **Tailwind CSS** (настраивается через `postcss.config.mjs` и глобальный CSS)
- **PNPM** для управления зависимостями

## 🚀 Установка и запуск

```bash
# клонировать репозиторий
git clone https://github.com/alexshescp/rusmasssurveilance.git
cd rusmasssurveilance

# установить зависимости
pnpm install

# запустить в режиме разработки
pnpm dev
```

> Сервер будет доступен по адресу `http://localhost:3000`.

## 🗂 Структура проекта

```
app/
  globals.css
  layout.tsx        # базовый layout с ThemeProvider и toast
  page.tsx          # корневая страница, рендерит основные секции

components/         # модули бизнес-логики и представления
  central-hub.tsx
  connections-section.tsx
  hero-section.tsx
  line-details.tsx
  metro-map.tsx
  particle-canvas.tsx
  theme-provider.tsx
  ui/                # набор UI-компонентов (кнопки, модалы, таблицы и пр.)

hooks/               # кастомные React-хуки
  use-mobile.ts
  use-toast.ts

lib/
  utils.ts           # вспомогательные утилиты

public/              # статические файлы

styles/
  globals.css        # глобальные стили
```

## 🧩 Компоненты UI

В директории `components/ui` находятся переиспользуемые элементы интерфейса: кнопки, формы, модальные окна, таблицы, списки и др. Они реализованы с помощью Tailwind и подходят для быстрой сборки страниц.

## 🔍 Хуки

- `use-mobile` — определяет прикрепление устройства, возвращает флаг `isMobile`.
- `use-toast` — обёртка над библиотекой уведомлений `sonner`, упрощает показ всплывающих сообщений.

## ⚙️ Настройка темы

`ThemeProvider` использует контекст и локальное состояние для переключения светлой/тёмной темы. Файл `theme-provider.tsx` обеспечивает root-обёртку.

## 📄 Лицензия

Укажите лицензию проекта здесь (например, MIT).

---

> Этот README генерирован автоматически на основе структуры проекта. Все упоминания о Vercel были исключены по запросу. В основе лежит React с TypeScript, набор компонентов UI и кастомные хуки.